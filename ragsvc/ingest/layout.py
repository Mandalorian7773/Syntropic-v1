"""Layout analysis. Owner: person 2. Empty by design.

Will contain: detection of tables, headers and figure captions so a chunk does
not straddle a table boundary. Refinery SOPs are mostly tables; getting this
wrong makes retrieval look broken when it is chunking that is broken.
"""
