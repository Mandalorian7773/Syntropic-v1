"""Retrieval evaluation harness. Owner: person 2. Empty by design.

Will contain: recall@k and MRR over eval/questions.jsonl, so that "we added a
reranker" becomes a number instead of a feeling. Writes results next to
bench/results/ and prints a one-line summary.

    python ragsvc/eval/retrieval_eval.py
"""

if __name__ == "__main__":
    raise SystemExit("retrieval_eval.py is a stub -- owner: person 2")
