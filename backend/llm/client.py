"""llama.cpp HTTP client. Owner: person 3. Empty by design.

Will contain: the streaming completion call against MODEL_ENDPOINT, token
extraction, and tool-call parsing. Talks to the local llama-server only --
never to a hosted API.
"""
